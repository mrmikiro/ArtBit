{
  "buildCommand": "flutter/bin/flutter build web --release",
  "installCommand": "git clone https://github.com/flutter/flutter.git && flutter/bin/flutter doctor",
  "outputDirectory": "build/web",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
